name: aws-validator-xen-ts
description: An X1 validator running on AWS using TypeScript
runtime:
  name: nodejs
  options:
    packagemanager: yarn
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
